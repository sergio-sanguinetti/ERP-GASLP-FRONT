# -----------------------------------------------------------------------------
# App
# -----------------------------------------------------------------------------
NEXT_PUBLIC_APP_URL=http://localhost:3000
BASEPATH=

# -----------------------------------------------------------------------------
# API Backend (para evitar Mixed Content en Vercel: front HTTPS → backend HTTP)
# -----------------------------------------------------------------------------
# URL pública que usa el frontend (puede ser HTTP en local).
NEXT_PUBLIC_API_URL=http://localhost:3001/api
# URL del backend para el proxy (solo servidor). En Vercel pon la URL HTTP del backend.
# BACKEND_API_URL=http://76.13.113.159:3001/api
